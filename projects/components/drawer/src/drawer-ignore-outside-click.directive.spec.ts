import { DrawerIgnoreOutsideClickDirective } from './drawer-ignore-outside-click.directive';

describe('DrawerIgnoreOutsideClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerIgnoreOutsideClickDirective();
    expect(directive).toBeTruthy();
  });
});
