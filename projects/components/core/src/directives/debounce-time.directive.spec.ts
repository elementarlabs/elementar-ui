import { DebounceTimeDirective } from './debounce-time.directive';

describe('DebounceTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceTimeDirective();
    expect(directive).toBeTruthy();
  });
});
